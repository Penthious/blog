// import * as types from '../constants/sentHistoryTypes';

const INITIAL_STATE = {};
export default function sentHistoryReducer(state = INITIAL_STATE, action) {
    switch (action.type) {
        default:
            return { ...state };
    }
}
